package com.example.mysql;

public class UniqueConstraint {
	
	/*
	 * syntax -> create table common.organization(id int, name varchar(25) unique);
	 * 
	 * or
	 * 
	 * alter table common.organization add constraint unique(name);
	 * 
	 * 
	 */

}
