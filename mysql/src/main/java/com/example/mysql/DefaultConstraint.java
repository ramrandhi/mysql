package com.example.mysql;

public class DefaultConstraint {
	
	/*
	 * to create a table
	 *   create table product (id int, name varchar(25), price decimal(8,2) default 0);
	 */
	
	/*
	 * 		default contraint to add after is created
	 * 
	 * 		alter table product alter price set default 0;
	 */
	
	/*
	 * 	when we set default some value to the column then if data is not entered that column then it sets to default value
	 */
}
