package com.example.mysql;

public class OrderByclause {
	
	/*
	 * 		used to order data in ascending or descending order
	 * 
	 * 		EXAMPLE : select * from employees order by last name;
	 * 		by default it is in ascending order but we can make it explicities
	 * 
	 * 		ASCENDING ORDER : select * from employees order by name asc;
	 * 		DESCENDING ORDER : SELECT * FROM EMPLOYEES ORDER BY NAME DESC;
	 *  
	 *  
	 *  	we can also sort in ascending or descending order 2 or more columns
	 *  
	 *  	select * from employees order by first_name asc, second_name desc;
	 */

}
