package com.example.mysql;

public class Functions {
	
	/*
	 * 1. count() -> select count(amount) as sum of amounts from transactions;
	 * 2. sum() -> select sum(amount) as sum of amounts from transactions;
	 * 3. max() -> select max(amount) as maximum amount from transactions;
	 * 4. min() -> select min(amount) as minimum amount from transactions;
	 * 5. concat() -> select concat(first_name, "", last_name) as full_name from employees;
	 * 6. average() -> select average(amount) as average amount from transactions;
	 */

}
